# Heavy Seeker Bringup
The configuration files (.yaml) and assosiated launch files are stored here.

## Usage
`ros2 launch hs_bringup main.launch.py` - launches all the connected sensors, creates nodes and topics for them.